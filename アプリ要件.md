# PWAシフト管理アプリ 要件定義

## 1. カレンダー表示

- 曜日行はカレンダー本体と別に表示する
- 日付セルの表示形式は `6行 × 7列` の固定とする
- 表示対象は以下の通りとする
  - 中央：当月
  - 左：前月（スワイプで表示）
  - 右：次月（スワイプで表示）

## 2. 日付セル仕様

各日付セルには以下を表示する。

- 日付
- 状態ボタン（1文字表示）

### 状態一覧

| 状態 | 意味 | 操作 |
| --- | --- | --- |
| 日 | 日勤 | 手動 |
| 夜 | 夜勤 | 手動 |
| 明 | 明け | 手動 |
| 休 | 休み | 手動 |

## 3. 状態変更ルール

### ユーザー操作

状態ボタン押下時は、以下の順で状態を切り替える。

`日 → 夜 → 明 → 休 → 日（ループ）`

### 自動制御

- 「夜」の翌日は自動で「明」に変更する
- 翌日に手動入力がある場合も、自動で「明」に強制上書きする
- ただし月をまたぐ場合は自動変更しない

## 4. 月またぎルール

- 月またぎの自動連動は行わない
- 前月末が「夜」であっても、翌月1日は自動変更しない
- 必要に応じてユーザーが手動で「明」を設定する

## 5. モード制御

画面右側の操作エリアに以下のラジオボタンを配置する。

- 編集モード
- ロックモード

### 動作仕様

| モード | 動作 |
| --- | --- |
| 編集 | 状態ボタン操作可能 |
| ロック | 状態ボタン操作不可 |

ロック状態時の仕様は以下の通りとする。

- 状態ボタンのみ操作不可とする
- ラジオボタン表示はグレーアウトする
- モード切替操作は可能とする
- クリアボタンは操作不可とする
- 共有ボタンは操作可能とする

## 6. 集計・給与計算

### 表示項目

- 日の個数
- 夜の個数
- 勤務回数（`日 + 夜`）

### 単価入力

- 日勤単価（入力欄）
- 夜勤単価（入力欄）

### 給与計算式

`日数 × 日勤単価 + 夜数 × 夜勤単価`

### 計算ルール

- 「明」はカウントしない
- 「休」は `0` として扱う
- 月を切り替えた際は、その月の保存済みデータを読み込んで再計算する
- 勤務回数は `日 + 夜` の合計とする

### 表示形式

- 月収はカンマ区切りで表示する

## 7. 共有機能

### 共有ボタン

- 月ヘッダーに `共有` ボタンを配置する
- 表示中の月のシフト内容をテキスト化してクリップボードにコピーする

### 共有テキスト形式

以下の形式で出力する。

```text
2026年4月
1日：日
2日：夜
3日：明
...
30日：休

日勤回数：x回
夜勤回数：y回
勤務回数：z回
```

- 出力対象は表示中の月のみとする
- 日数は実在する月末日まで出力する
- 区切り記号は全角コロン `：` を使用する
- コピー失敗時は画面上のステータスで通知する

## 8. データ保存

### 保存方式

- `localStorage` を使用する

### 保存単位

- 月単位で勤務データを保存する
- 日勤単価・夜勤単価・ロック/編集モードは共通設定として別キーで保存する

### データ構造

```json
{
  "2026-03": {
    "shifts": ["休", "日", "夜", "明", "休", "日", "..."]
  },
  "2026-04": {
    "shifts": ["日", "日", "休", "夜", "..."]
  },
  "__sharedSettings": {
    "dayRate": "10000",
    "nightRate": "23400",
    "mode": "edit"
  }
}
```

- `shifts` の配列インデックスは日付を表す（`0 = 1日`）
- 月データ読込時は、その月の日数に合わせて `shifts` 配列を正規化する

## 9. UI / UX仕様

### 今日の表示

- 当日は視覚的に強調表示する

### 月切替操作

- 左スワイプで次月へ移動する
- 右スワイプで前月へ移動する
- 前月ボタン / 次月ボタンでも移動可能とする

### 補助操作

- 月ヘッダーに `共有` / `前月` / `次月` / `クリア` ボタンを表示する
- `クリア` は表示中の月の状態をすべて `休` にする

### ボタンサイズ

- 画面幅に収まる範囲で最大サイズに自動調整する
- 狭い画面幅ではヘッダーのボタン群を折り返し表示可能とする

### スクロール

- 縦スクロールを許可する

## 10. PWA要件

### 対応環境

- iPhone と Android の両方で利用できることを前提とする
- スマートフォンのブラウザ上で動作し、ホーム画面追加後の利用にも対応する

### 基本機能

- オフライン動作可能
- ホーム画面追加対応
- 端末種別に応じてホーム画面追加方法を画面上で案内する
- Android ではインストールボタン表示に対応する場合がある
- iPhone では Safari の共有メニュー経由でホーム画面追加する案内を表示する

### 起動時表示

- 当月カレンダーを表示する

### アイコン

- `192×192`
- `512×512`
- 作成済み

## 実装時の注意

以下は仕様補足として、実装時に必ず考慮すること。

### 1. 「夜→明」の自動処理タイミング

- ボタン押下時に即時反映する
- ただし翌日が存在する場合のみ実行する

### 2. 自動上書きの優先順位

- 夜をセットしたら翌日は強制で「明」とする
- 翌日に手動入力がある場合も強制上書きする
- 翌日がすでに「明」の場合はそのままでよい

### 3. `localStorage` 更新タイミング

- ボタン押下ごとに保存する
- 単価入力変更時も保存する
- モード変更時も保存する
- クリア実行時も保存する
